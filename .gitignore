# Local MCP credentials — never commit (contains a live API key)
.mcp.json

# Obsidian — keep shared vault config, ignore per-machine UI state
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# Node / Next.js app
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Env
.env
.env*.local
.vercel

# OS
.DS_Store
Thumbs.db
